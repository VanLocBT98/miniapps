# Admin

Independent mini application following the BASIC-APP layout.

## Develop standalone

```bash
pnpm --filter @repo/admin dev
```

## Mount in Main App

Export: `src/project.ts` via `createProject()`.
