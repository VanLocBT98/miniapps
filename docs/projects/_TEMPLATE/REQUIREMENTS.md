# <Project Name> — Requirements (living / future-facing)

> Status: `draft` | `active` | `frozen`  
> Last updated: YYYY-MM-DD  
> Package: `@repo/<id>` · Code: `projects/<id>`  
> **Rule**: Chỉ ghi những gì còn đúng cho **hiện tại + tương lai gần**. Việc đã làm → `CHANGELOG.md`. Spec lớn → `features/<slug>.md`.

## North star

Một câu: project này phải mang lại gì.

## Current focus (next / in-flight)

- [ ] …   ← chỉ việc sắp làm / đang làm
- Spec chi tiết (nếu có): `features/<slug>.md`

## Standing constraints (vẫn phải giữ)

Ràng buộc **còn hiệu lực** (stack, auth, style tokens, public route…). Không liệt kê lịch sử feature.

- Align `AGENTS.md`
- …

## Done baseline (optional, tối đa ~5–10 gạch)

Chỉ liệt kê capability **ổn định** mà agent cần biết để không phá — không viết lại acceptance cũ.

- …

## Out of scope (hiện tại)

- …

## Open questions

- …
